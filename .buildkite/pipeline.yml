agents:
  queue: cloud-m4xlarge

steps:
  - label: TFLint
    plugins:
      - docker#v3.8.0:
          image: "ghcr.io/terraform-linters/tflint:latest"
          command: [ "--recursive" ]
          workdir: "/workdir"
          volumes:
            - ".:/workdir"
